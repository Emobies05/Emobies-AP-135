Deploy success → https://emobies.com → {"status":"Emobies Backend SSL 1.0.2 Live!"}
App connects → Ernakulam repairs live
const express = require('express');
const cors = require('cors');
const app = express();
app.use(cors());
app.use(express.json());
app.get('/', (req,res)=>res.json({status:'Emobies Backend SSL 1.0.2 Live!'}));
app.get('/api/health', (req,res)=>res.json({service:'ernakulam', status:'ready'}));
app.listen(process.env.PORT || 3000, ()=>console.log('Emobies API running'));
